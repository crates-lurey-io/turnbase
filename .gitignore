/target
.matan/

# Formatter/linter toolchain for tools/package.json. The lockfile IS tracked (and marked
# linguist-generated in .gitattributes); only the installed tree is not.
node_modules/

# Coverage output from `just coverage-gen`.
/lcov.info

# Byte-compiled output from tools/*.py.
__pycache__/

# Generated by tools/gen-llms-txt.sh (cargo-llms-txt writes these to the CWD);
# the docs site regenerates them into target/doc, so the root copies are just
# transient build output.
/llms.txt
/llms-full.txt
