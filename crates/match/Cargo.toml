[package]
name = "turnbase-match"
description = "Turn-loop orchestration for the Turnbase engine: seat agents and a match stepper"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true
readme = "README.md"

[lints]
workspace = true

# Document every feature-gated item on docs.rs, not just the default build.
[package.metadata.docs.rs]
all-features = true

[dependencies]
turnbase = { workspace = true }
turnbase-bots = { workspace = true }
log = { workspace = true }
