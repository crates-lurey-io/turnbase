[package]
name = "turnbase-bots"
description = "Search and policy bots for the Turnbase engine (random, minimax, MCTS)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
turnbase = { path = "../core" }

[dev-dependencies]
tic_tac_toe = { path = "../../examples/tic_tac_toe" }
high_card = { path = "../../examples/high_card" }
coup = { path = "../../examples/coup" }
proptest = "1"
