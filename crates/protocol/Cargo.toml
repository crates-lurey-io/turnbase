[package]
name = "turnbase-protocol"
description = "Transport-agnostic request/response wire types for the Turnbase engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
